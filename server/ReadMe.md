morgan - which is a HTTP request logger middleware for node.js (remember to add types: npm i --save-dev @types/morgan)

ngrok - that allows us to expose local servers to the internet. In one of the functions we shall have, Safaricom requires that one has a Callback URL. this URL does not accept local server links, http://localhost:5000 for instance. This is where ngrok comes in.

dayjs - minimalist JS library for date manipulation.